#include <stdio.h>
int main()
{
    float m=98.5; // maths no
    float e=88.6; // english number
    float p=92; // number in physics
    float c=89; // number in chemistry
    float co=88; //number in computer
    float percentage = (m+e+p+c+co)/5;
    printf("your percentage is: %f",percentage);
    return 0;
}